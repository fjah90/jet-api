import { faker } from '@faker-js/faker';

export const MockedPrismicRemoteRepository = {
  getAirports: jest.fn(() => true),
};

export const MockedPrismicService = {
  getAirports: jest.fn(() => true),
};
